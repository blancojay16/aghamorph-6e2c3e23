import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.aghamorph.app',
  appName: 'Aghamorph',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};
export default config;
